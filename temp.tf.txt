   # Install OS updates, update and upgrade existing apps.
   provisioner "remote-exec" {
    connection {
      host = self.public_ip
      type = "ssh"
      user = "ubuntu"
	  private_key = file("/Users/lancer/workspace/virtual-lab/privatekey.txt")
    }
    inline = [
    	"sudo DEBIAN_FRONTEND=noninteractive apt-get -yqq dist-upgrade",   	
    ]
  }


This worked!:
sudo DEBIAN_FRONTEND=noninteractive apt-get -yqq -o Dpkg::Options::='--force-confdef' -o Dpkg::Options::='--force-confold' dist-upgrade


    provisioner "remote-exec" {
    connection {
      host = self.public_ip
      type = "ssh"
      user = "ubuntu"
	  private_key = file("/Users/lancer/workspace/virtual-lab/privatekey.txt")
    }
    inline = ["sudo reboot +1"]
    #inline = ["echo $HOME"]
  }
